<Window x:Class="StokTakip.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SYA Stok Takip Sistemi"
        Width="900"
        Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="60,70,40,70" VerticalAlignment="Center">
            <TextBlock Text="SYA Stok Takip Sistemi"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextDarkBrush}" />
            <TextBlock Text="Kurumsal stok takip giriş ekranı"
                       Margin="0,12,0,0"
                       FontSize="16"
                       Foreground="{DynamicResource TextMutedBrush}" />
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,36,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="ERP stok yönetimi"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextDarkBrush}" />
                    <TextBlock Text="Ürün, stok, kritik seviye ve rapor modüllerine modern masaüstü arayüzünden erişin."
                               Margin="0,8,0,0"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextMutedBrush}" />
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Column="1"
                Style="{StaticResource CardBorderStyle}"
                Width="330"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Giriş Yap"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextDarkBrush}" />
                <TextBlock Text="Kullanıcı bilgilerinizi giriniz."
                           Margin="0,6,0,24"
                           Foreground="{DynamicResource TextMutedBrush}" />

                <TextBlock Text="Kullanıcı adı" Style="{StaticResource FormFieldLabelStyle}" />
                <TextBox x:Name="TxtKullaniciAdi" Margin="0,0,0,16" Text="admin" />

                <TextBlock Text="Şifre" Style="{StaticResource FormFieldLabelStyle}" />
                <PasswordBox x:Name="PwdSifre" Margin="0,0,0,8" Password="1234" />

                <TextBlock x:Name="TxtGirisDurumu"
                           Text="Giriş yapılıyor, lütfen bekleyin..."
                           Visibility="Collapsed"
                           Margin="0,0,0,12"
                           FontSize="12"
                           Foreground="{DynamicResource PrimaryBrush}" />

                <Button x:Name="BtnGiris"
                        Content="Giriş Yap"
                        MinHeight="42"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="BtnGiris_Click" />
                <Button x:Name="BtnCikis"
                        Content="Çıkış"
                        Margin="0,10,0,0"
                        MinHeight="38"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnCikis_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
